# Copy to .streamlit/secrets.toml for local use, or paste the same line into
# Streamlit Cloud under  App settings → Secrets.
# Never commit the real file — .gitignore already excludes it.
ANTHROPIC_API_KEY = "sk-ant-..."
